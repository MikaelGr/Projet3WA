<%= form_with(model: booster, local: true) do |form| %>
  <% if booster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booster.errors.count, "error") %> prohibited this booster from being saved:</h2>

      <ul>
      <% booster.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :price %>
    <%= form.number_field :price, class:"form-control" %>
  </div>

  <div class="actions">
    <%= form.submit class:'btn btn-success' %>
  </div>
<% end %>
